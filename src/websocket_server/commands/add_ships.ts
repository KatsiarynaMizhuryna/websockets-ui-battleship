// import { Ship, games } from "../../utils/models.js";

// export const addShips = (data: any) => {
//   const { gameId, ships, indexPlayer } = JSON.parse(data);
//   const gameData = games.find((game) => game.gameId === gameId);

//   gameData?.ships.push(ships);
//   console.log("gameData", gameData);
// };
